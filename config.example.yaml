env: dev
service_name: Exemple

jwt:
  secret_key: ""
  alg: HS256

cookie:
  # Cookie domain for cross-subdomain cookies (production only)
  # Examples:
  #   - ".yourdomain.com" - works on api.yourdomain.com and crm.yourdomain.com
  #   - "" (empty) - works only on same domain (localhost for dev)
  domain: ""

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  sslmode: disable

redis:
  host: localhost
  port: 6379

email:
  login: ""
  password: ""
  host: ""
  port: ""
  ssl: false
  from_address: ""
  from_name: ""

nats:
  host: localhost
  port: 4222

serves:
  auth: "http://localhost:8080"
  users: "http://localhost:8081"
  housing: "http://localhost:8082"
  tasks: "http://localhost:8083"
  notification: "http://localhost:8084"
